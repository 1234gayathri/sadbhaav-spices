services:
  - type: web
    name: sadbhaav-spices
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/server/server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: sadbhaav-db
      mountPath: /data
      sizeGB: 1
